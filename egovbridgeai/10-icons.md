# Phase 10 — Icons

> A reference table, not a feature list. Dashboard/Submit Report/My
> Reports/AI Assistant/Summarizer/Logout are already wired to these exact
> icons in `nav-links.tsx` and `layout.tsx` (Phase 9). `User` now backs the
> avatar fallback on `/dashboard` for users with no name/phone to derive
> initials from. `Bell`/`Notifications`, `Search`, and `Settings` have no
> feature behind them in any phase built so far — reserved for later.

| Feature | Lucide Icon |
|----------|-------------|
| Dashboard | LayoutDashboard |
| Submit Report | FileText |
| My Reports | ClipboardList |
| AI Assistant | Bot |
| Summarizer | Sparkles |
| User | User |
| Notifications | Bell |
| Logout | LogOut |
| Search | Search |
| Settings | Settings |
