# Phase 9 — Navigation

> Implemented as `app/dashboard/layout.tsx` (a Next.js layout, so it wraps
> every `/dashboard/*` page automatically) + `nav-links.tsx` for the
> active-link highlighting. Logout moved here from the dashboard page itself
> (Phase 3), since previously there was no way to log out from
> `/dashboard/reports`, `/assistant`, etc.

```
Dashboard
│
├── Home
├── Submit Report
├── My Reports
├── AI Assistant
├── Document Summarizer
└── Logout
```
